/*
 * RinksPage Messages
 *
 * This contains all the text for the RinksPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.RinksPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: '',
  },
  legend: {
    id: `${scope}.legend`,
    defaultMessage: '',
  },
});
