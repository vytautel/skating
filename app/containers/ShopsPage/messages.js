/*
 * ShopsPage Messages
 *
 * This contains all the text for the ShopsPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ShopsPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: '',
  },
});
