/*
 * RinksPage Messages
 *
 * This contains all the text for the RinksPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.layout.game';

export default defineMessages({
  continue: {
    id: `${scope}.continue`,
    defaultMessage: '',
  },
  correct: {
    id: `${scope}.correct`,
    defaultMessage: '',
  },
  correctCount: {
    id: `${scope}.correctCount`,
    defaultMessage: '',
  },
  mistakestCount: {
    id: `${scope}.mistakestCount`,
    defaultMessage: '',
  },
});
