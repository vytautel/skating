/*
 * RinksPage Messages
 *
 * This contains all the text for the RinksPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.layout.info';

export default defineMessages({
  logo: {
    id: `${scope}.logo`,
    defaultMessage: '',
  },
  timetableAndInfo: {
    id: `${scope}.timetableAndInfo`,
    defaultMessage: '',
  },
  branches: {
    id: `${scope}.branches`,
    defaultMessage: '',
  },
});
